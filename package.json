{
  "name": "urglowup",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter @urglowup/db run generate && pnpm --filter web build",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "test:e2e": "pnpm --filter web test:e2e"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
