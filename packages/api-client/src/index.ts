// Typed fetch SDK for /api/v1, consumed by the Expo mobile app (apps/mobile) and
// optionally the web app. Populated in Phase 7 (Expo scaffold) once Phase 3 (API v1)
// endpoints exist to call.

export {};
