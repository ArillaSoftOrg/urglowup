// Isomorphic Zod schemas + DTO types, shared by packages/domain, the web API v1
// routes, and the Expo mobile app. Populated starting in Phase 2 (migrating the
// per-action inline schemas) and Phase 3 (API v1) of the master implementation plan.

export {};
